# n8n + FFmpeg on Render

This repo deploys n8n with FFmpeg installed using Docker.

## Files

- Dockerfile — builds n8n and installs FFmpeg
- .dockerignore — keeps the image clean

## Deploy on Render

1. Create a new Web Service
2. Select this GitHub repo
3. Choose "Docker" as the environment
4. Deploy
